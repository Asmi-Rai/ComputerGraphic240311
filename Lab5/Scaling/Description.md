 **Scaling**:
**Scaling in graphics** is the process of enlarging or shrinking an object by multiplying its coordinates with scaling factors along the x, y (and z in 3D) axes. It changes the size of the object but keeps its shape proportional unless different factors are used for each axis.
